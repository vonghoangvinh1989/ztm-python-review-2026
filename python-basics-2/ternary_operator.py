# Ternary Operator
# "action if condition true" if condition else "action of condition not true"

is_friend = True
can_message = "message allowed" if is_friend else "not allowed to message"

print(can_message)
